@extends('layouts.app')

@section('content')
    <div class="container">
        <div class="card">
            <div class="card-header">
                <h5 class="card-title">Recently Added Questions</h5>
            </div>
            <div class="card-deck">

                @foreach($latestQuestions as $question)
                <div class="card mb-3 ">

                    <div class="card-body">
                        <p class="card-text">{{$question->body}}</p>
                        <a href="#" class="btn btn-primary float-right">View Question</a>
                    </div>

                </div>
        @endforeach
            </div>
    </div>

@endsection